// Hook receiver sidecar -- placeholder for future HTTP hook receiver binary.
// This will be a separate binary that receives webhook events and forwards
// them to the main Norbert application.
//
// Implementation deferred to a later step.

fn main() {
    eprintln!("norbert-hook-receiver: not yet implemented");
    std::process::exit(1);
}
