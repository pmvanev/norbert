/// Provider adapters for normalizing tool-specific events.
///
/// Each provider module implements EventProvider for a specific tool.
/// Provider-specific types are private to each submodule.
pub mod claude_code;
