{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the Norbert desktop application",
  "windows": ["main", "window-*"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-destroy"
  ]
}
