/**
 * @norbert/config -- Configuration loading and validation.
 *
 * Independent module. No dependencies on other @norbert/* packages.
 * Loads configuration from ~/.norbert/config.json, provides defaults.
 */

export {};
